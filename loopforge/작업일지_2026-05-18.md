# LoopForge AI × Connect AI — 작업일지
> 날짜: 2026-05-18 | 담당: Claude Code (claude-sonnet-4-6)

---

## 1. Connect AI (VS Code 확장) — extension.ts import 수정

### 문제
`src/extension.ts` (21,724줄) 파일에서 모듈 분리 후 `import` 문이 파일 **중간**에 위치해 TypeScript 컴파일 오류 발생.

| 라인 | import | 문제 |
|---|---|---|
| 702번 | `./paths` | 파일 중간 → 파싱 오류 |
| 784번 | `./agents` | 파일 중간 → 파싱 오류 |
| 1359번 | `./system-specs` | 파일 중간 → 파싱 오류 |

### 해결
세 import를 파일 **상단(1~10번 줄)**으로 이동.

```typescript
// 수정 전: 파일 중간에 흩어진 import
// 702번 줄: import { _getBrainDir, ... } from './paths';
// 784번 줄: import { AgentDef, AGENTS, ... } from './agents';
// 1359번 줄: import { SystemSpecs, ... } from './system-specs';

// 수정 후: 파일 상단에 통합
import * as vscode from 'vscode';
import * as http from 'http';
import axios from 'axios';
import * as fs from 'fs';
import * as path from 'path';
import * as os from 'os';
import { spawn, spawnSync } from 'child_process';
import { _getBrainDir, _isBrainDirExplicitlySet, getCompanyDir, COMPANY_SUBDIR, _expandTilde, _resolvePathInput } from './paths';
import { AgentDef, AGENTS, AGENT_ORDER, SPECIALIST_IDS } from './agents';
import { SystemSpecs, getSystemSpecs, estimateModelMemoryGB } from './system-specs';
```

---

## 2. LoopForge AI 랜딩페이지 — Tasks A~E 완료

### Task A — index.html 수정

| 항목 | 내용 |
|---|---|
| **Turnstile 위젯** | `YOUR_SITE_KEY_HERE` → `0x4AAAAAADReGXn-318jsskf` 교체 |
| **GA4 측정 ID** | `G-XXXXXXXXXX` → `G-NMYD46BR86` 교체 |
| **GA4 스크립트** | `</head>` 앞에 gtag 스크립트 삽입 |
| **form_submit 이벤트** | 폼 성공 시 `gtag('event','form_submit')` 발동 |
| **Turnstile 토큰 검증** | `submitForm()` 에 토큰 수집 + 미완료 시 에러 처리 |
| **카카오 URL** | `http://` → `https://` 전면 교체 |

**PowerShell로 로컬 파일 적용:**
```powershell
cd "D:\1인기업\LoopForge AI"

# Turnstile 위젯 삽입
$content = Get-Content "index-v6-final.html" -Encoding UTF8 -Raw
$content = $content -replace '<!-- Turnstile 준비중 -->', '<div class="cf-turnstile" data-sitekey="0x4AAAAAADReGXn-318jsskf" data-theme="dark"></div>'
[System.IO.File]::WriteAllText("$PWD\index-v6-final.html", $content, (New-Object System.Text.UTF8Encoding $False))

# GA4 스크립트 삽입
$content = Get-Content "index-v6-final.html" -Encoding UTF8 -Raw
$ga4Script = @"
<!-- Google Analytics 4 -->
<script async src="https://www.googletagmanager.com/gtag/js?id=G-NMYD46BR86"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());
  gtag('config', 'G-NMYD46BR86');
</script>
</head>
"@
$content = $content -replace '</head>', $ga4Script
[System.IO.File]::WriteAllText("$PWD\index-v6-final.html", $content, (New-Object System.Text.UTF8Encoding $False))
```

---

### Task B — kakao_alimtalk.js (신규 작성)

**파일:** `loopforge/kakao_alimtalk.js`

| 기능 | 내용 |
|---|---|
| `POST /` | 단건 알림톡 발송 |
| `POST /n8n` | n8n 웹훅 핸들러 (자동 2종 발송) |
| 템플릿 1 | 사장님 → 신규 문의 알림 |
| 템플릿 2 | 신청자 → 접수 확인 알림 |
| CORS | OPTIONS preflight 처리 |

**Cloudflare Worker 배포:**
```powershell
# wrangler 설치
npm install -g wrangler
wrangler login

# Worker 배포
cd "D:\1인기업\LoopForge AI"
wrangler deploy kakao_alimtalk.js --name loopforge-kakao-alimtalk --compatibility-date 2026-01-01

# 환경변수 설정 (심사 완료 후)
wrangler secret put KAKAO_ACCESS_TOKEN   --name loopforge-kakao-alimtalk
wrangler secret put KAKAO_SENDER_KEY     --name loopforge-kakao-alimtalk
wrangler secret put KAKAO_PLUS_FRIEND_ID --name loopforge-kakao-alimtalk
# 입력값: _wLasX
wrangler secret put OWNER_PHONE          --name loopforge-kakao-alimtalk
# 입력값: 사장님 전화번호 (예: 01012345678)
```

---

### Task C — Cloudflare Worker 구조 분석

#### cf-worker-review-api.js
- **역할:** 리뷰 JSON → OpenAI `gpt-4o-mini` 분석 → 결과 반환
- **인증:** `X-Webhook-Secret` 헤더
- **마스킹:** 작성자 이름(첫/끝 글자), 전화번호(중간 마스킹)
- **처리:** 최대 5개씩 병렬 배치
- **반환:** 개별 분석 + 집계 리포트 (부정 건수, 평균 감정 점수, 상위 키워드 10개)
- **상태:** ✅ 이슈 없음

#### cf-worker-webhook-proxy.js
- **역할:** 클라이언트 → n8n 웹훅 프록시 (n8n URL 은닉)
- **CORS:** `ALLOWED_ORIGINS` 환경변수로 동적 제어
- **인증:** `X-Webhook-Secret` + `WEBHOOK_SECRET` 환경변수
- **n8n 인증:** `N8N_AUTH_HEADER` 환경변수 지원
- **상태:** ✅ 이슈 없음

#### 전체 데이터 플로우
```
[문의 폼]
  → loopforge-contact-proxy (공개 엔드포인트 + Turnstile)
  → n8n 웹훅
  → Google Sheets 저장
  → kakao_alimtalk Worker
  → 사장님 알림톡 + 신청자 확인 알림톡

[리뷰 분석]
  → loopforge-review-api (X-Webhook-Secret 인증)
  → OpenAI gpt-4o-mini
  → 분석 결과 반환
```

---

### Task D — Cloudflare Pages 배포 파일

#### `_headers`
```
/*
  X-Frame-Options: DENY
  X-Content-Type-Options: nosniff
  Referrer-Policy: strict-origin-when-cross-origin
  Permissions-Policy: camera=(), microphone=(), geolocation=()

/index.html
  Content-Security-Policy: default-src 'self'; script-src 'self' 'unsafe-inline' https://challenges.cloudflare.com https://www.googletagmanager.com; style-src 'self' 'unsafe-inline'; connect-src 'self' https://*.workers.dev https://www.google-analytics.com; img-src 'self' data: https:; frame-src https://challenges.cloudflare.com
  Cache-Control: no-cache

/*.js
  Cache-Control: public, max-age=31536000, immutable

/*.css
  Cache-Control: public, max-age=31536000, immutable
```

#### `_redirects`
```
/           /index.html   200
/home       /             301
/contact    /#contact     302
/price      /#price       302
/pricing    /#price       302
/faq        /#faq         302
/*          /index.html   404
```

---

### Task E — 비교평가 리포트 v7

**파일:** `loopforge/LoopForge_비교평가_벤치마킹_리포트_v7.md`

| 항목 | v6 | v7 |
|---|:---:|:---:|
| CRO | 9.5 | **9.8** |
| 카피라이팅 | 9.5 | 9.5 |
| 신뢰/사회적 증거 | 9 | 9 |
| 모바일 UX | 9.5 | 9.5 |
| 디자인 | 9 | 9 |
| 보안 | 9 | **9.5** |
| SEO | 9 | 9 |
| 상품 사다리 | 9.5 | 9.5 |
| 속도 | 9 | **9.2** |
| 접근성 | 8.5 | 8.5 |
| **종합 평균** | **9.15** | **9.35** |

---

## 3. Git 커밋 이력

| 커밋 | 내용 |
|---|---|
| `6b4edf8` | fix: extension.ts 중간 import → 상단 이동 |
| `b49b62f` | feat: index.html Turnstile+GA4, kakao_alimtalk.js |
| `0b7e3c6` | fix: GA4 측정 ID 교체 — G-NMYD46BR86 |
| `e2bf028` | fix: Turnstile sitekey 교체 — 0x4AAAAAADReGXn-318jsskf |

**PR:** https://github.com/wnsrua01-create/wnsrua01-create/pull/4

---

## 4. 남은 작업 체크리스트

### 즉시
- [x] Turnstile sitekey 교체 (`0x4AAAAAADReGXn-318jsskf`)
- [x] GA4 Measurement ID 교체 (`G-NMYD46BR86`)
- [ ] `git push` → Cloudflare Pages 자동 배포 확인
- [ ] loopforge-contact-proxy Worker에 Turnstile 서버사이드 검증 추가

### 카카오 심사 완료 후 (3~7 영업일)
- [ ] `kakao_alimtalk.js` Worker 배포
- [ ] 환경변수 4개 설정 (KAKAO_ACCESS_TOKEN, KAKAO_SENDER_KEY, KAKAO_PLUS_FRIEND_ID, OWNER_PHONE)
- [ ] n8n 워크플로우에 `/n8n` 엔드포인트 연결

### 이번 주
- [ ] OG 이미지 생성 (1200×630px) → `og-image.png` 업로드
- [ ] Google Search Console 등록
- [ ] Google Maps API 키 도메인 제한 설정
- [ ] Cloudflare Analytics 설정

### 다음 달
- [ ] Stripe 실결제 플로우 완성
- [ ] A/B 테스트: Hero 헤드라인 2가지 버전
- [ ] 실제 고객 후기로 교체

---

## 5. 보안 주의사항

> ⚠️ **Google Maps API 키** (`AIzaSyBH...`) — 도메인 제한 미설정 상태.  
> Cloudflare Dashboard → APIs & Services → 사용자 인증 정보 → 키 편집 → HTTP 리퍼러 제한 추가 필수.

> ⚠️ **WEBHOOK_SECRET** — 절대 클라이언트 코드에 노출하지 말 것.  
> contact-proxy는 공개 엔드포인트 → Turnstile 서버사이드 검증으로 봇 차단.
